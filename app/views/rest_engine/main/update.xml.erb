<%=
    {
        :success => @success,
        :message => @success ? "Updated #{@resources} #{@item.id}" : "Failed to update #{@resources} #{@item.id}",
        @resources => [@item]
    }.to_xml(:include => @associations).html_safe
-%>
