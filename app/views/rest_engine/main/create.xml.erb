<%=
    {
        :success => @success,
        :message => @success ? "Created new #{@resources} #{@item.id}" : "Failed to create #{@resources}",
        @resources => @success ? [@item] : []
    }.to_xml.html_safe
-%>